import { render, screen } from '@testing-library/react';
import App from './App';

test('Exibe o título "Piadas do Chuck Norris" na tela', () => {});

test('Exibe o corpo da piada na tela', () => {});
